apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-pvc
  namespace: default
  labels:
    upgrades.dev/testing: good-news
    env: test
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Mi
